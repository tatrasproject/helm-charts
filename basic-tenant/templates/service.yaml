apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-tenant.fullname" . }}
  labels:
    {{- include "basic-tenant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basic-tenant.selectorLabels" . | nindent 4 }}
