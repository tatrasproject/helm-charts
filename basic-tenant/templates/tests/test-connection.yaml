apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basic-tenant.fullname" . }}-test-connection"
  labels:
    {{- include "basic-tenant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "basic-tenant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
